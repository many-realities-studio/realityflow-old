version: "3.4"
services:
  api:
    image: api:prod
    build:
      context: .
      target: build
    container_name: api
    volumes:
      - "./server/src:/usr/src:delegated"
      - /usr/src/node_modules
    command: ["npm", "run", "debug"]
