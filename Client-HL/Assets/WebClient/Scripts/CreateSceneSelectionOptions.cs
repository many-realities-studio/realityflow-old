using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CreateSceneSelectionOptions : MonoBehaviour {

	// Use this for initialization
	void Start () {
		//Ask database for list of scenes
        //For each scene in database, create button w/ name
	}
	
	// Update is called once per frame
	void Update () {

        //check if any scenes were added
		
	}
}
